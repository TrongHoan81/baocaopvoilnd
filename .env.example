# === Cấu hình proxy Render -> VPS ===
# Render: đặt PROXY_DOWNLOAD_VIA_VPS=1 để proxy; VPS/local: đặt 0 để chạy trực tiếp
PROXY_DOWNLOAD_VIA_VPS=0

# URL public của VPS (chỉ cần cho Render). Ví dụ: https://vps.pvoilnd.vn
VPS_BASE_URL=

# Khoá API nội bộ giữa Render <-> VPS (đặt chuỗi khó đoán, không chia sẻ công khai)
VPS_INTERNAL_API_KEY=

# Thời gian chờ cho phiên tải báo cáo (giây). Nên đủ dài vì tải/ghi GDrive có thể chậm
PROXY_TIMEOUT_SECONDS=1200
